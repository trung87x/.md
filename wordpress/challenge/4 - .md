##

Tuyệt vời! Chúng ta sẽ tiến hành thực hiện **Cấu trúc Grid và Container** để theme của bạn bắt đầu có bố cục responsive chuẩn của Bootstrap.

Thử thách này liên quan đến việc chỉnh sửa các file sau trong thư mục theme của bạn: **`header.php`**, **`footer.php`**, và **`index.php`**.

---

## Thử thách: Cấu trúc Grid và Container

### 1\. 📝 Cấu hình `header.php`

##

Mục tiêu là chèn thẻ meta `viewport` và mở các thẻ HTML cần thiết, bao gồm cả một thẻ `div` sẽ chứa nội dung chính của trang.

Bạn mở file **`header.php`** và thực hiện các thay đổi sau:

- **Thêm Viewport Meta Tag:** Đảm bảo tính responsive của Bootstrap hoạt động.

  PHP

      // header.php

      <meta charset="<?php bloginfo( 'charset' ); ?>">
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
      <link rel="profile" href="https://gmpg.org/xfn/11">

      <?php wp_head(); ?>
      </head>

      <body <?php body_class(); ?>>
      <?php wp_body_open(); ?>
      <div id="page" class="site">

          <a class="skip-link screen-reader-text" href="#content"><?php esc_html_e( 'Skip to content', 'mytwentyfive' ); ?></a>

          <header id="masthead" class="site-header">
              </header><div id="content" class="site-content container mt-4">
              <div id="primary" class="content-area">
                  <main id="main" class="site-main">

- **Giải thích:**

  - **`<div id="content" class="site-content container mt-4">`**: Chúng ta đã thêm class **`container`** để bao bọc nội dung, giới hạn chiều rộng và căn giữa. Class **`mt-4`** (margin-top 4) là của Bootstrap, tạo khoảng cách giữa header và nội dung.
  - **Lưu ý:** Chúng ta mở `<main id="main" class="site-main">` ở cuối `header.php`.

---

### 2\. 📄 Cấu hình `footer.php`

##

Mục tiêu là đóng tất cả các thẻ HTML đã mở trong `header.php` và các file mẫu khác.

Bạn mở file **`footer.php`** và thực hiện các thay đổi sau:

PHP

    // footer.php

                    </main></div></div><footer id="colophon" class="site-footer bg-light mt-5 p-4">
                <div class="container">
                    <div class="site-info">
                        <a href="<?php echo esc_url( __( 'https://wordpress.org/', 'mytwentyfive' ) ); ?>">
                            <?php
                            /* translators: %s: CMS name, i.e. WordPress. */
                            printf( esc_html__( 'Proudly powered by %s', 'mytwentyfive' ), 'WordPress' );
                            ?>
                        </a>
                        <span class="sep"> | </span>
                        <?php
                        /* translators: 1: Theme name, 2: Theme author. */
                        printf( esc_html__( 'Theme: %1$s by %2$s.', 'mytwentyfive' ), 'mytwentyfive', '<a href="#">Me</a>' );
                        ?>
                    </div></div></footer></div><?php wp_footer(); ?>

        </body>
        </html>

---

### 3\. 🧩 Cấu hình `index.php` (Áp dụng Grid)

##

Bây giờ bạn có thể sử dụng hệ thống Grid **(`row` và `col`)** của Bootstrap trong file mẫu. Chúng ta sẽ tạo bố cục **8 cột cho Nội dung** và **4 cột cho Sidebar** (tổng 12 cột) trên màn hình máy tính bảng trở lên.

Bạn mở file **`index.php`** và thay thế nội dung hiện tại (sau `get_header();`) bằng đoạn code sau:

PHP

    // index.php

    <?php
    get_header();
    ?>

    <div class="row">

        <div class="col-md-8">
            <?php
            if ( have_posts() ) :
                /* Bắt đầu vòng lặp WordPress */
                while ( have_posts() ) :
                    the_post();
                    get_template_part( 'template-parts/content', get_post_type() );
                endwhile;

                // Phân trang
                the_posts_navigation();

            else :
                get_template_part( 'template-parts/content', 'none' );
            endif;
            ?>
        </div><div class="col-md-4">
            <?php get_sidebar(); ?>
        </div></div><?php
    get_footer();

### ✨ Kiểm tra Kết quả

##

Sau khi lưu các file này, hãy kiểm tra trang web cục bộ của bạn:

1.  Nội dung và Sidebar sẽ được căn giữa màn hình (nhờ `container`).
2.  Trên màn hình rộng, bạn sẽ thấy Nội dung chiếm 2/3 và Sidebar chiếm 1/3 trang.
3.  Trên điện thoại, Sidebar sẽ tự động chuyển xuống dưới Nội dung (nhờ class `col-md-*`).
